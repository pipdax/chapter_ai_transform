/**
 * 内置 Prompt 模板列表
 */
export const BUILTIN_PROMPTS = [
  {
    id: 'summary-card',
    name: '📋 摘要卡片',
    description: '提取核心观点，生成结构化摘要卡片',
    template: `设计一个"网页文章可视化重构器"。

你的任务是：
将输入的网页文章内容抽取核心信息，
并转化为一个"极简现代、高可读性、高信息密度"的可视化阅读卡片页面。

------------------------------------------------------------
【输出要求（必须严格遵守）】

1. 你必须且只能输出 HTML 代码，不允许输出任何解释、描述、多余文字，用\`\`\`html...\`\`\`包裹输出的HTML代码。
2. 输出的内容去掉markdown包裹标签后，可以渲染
3. 不要污染宿主页面。

------------------------------------------------------------
【设计风格（必须保持一致）】

整体视觉风格为：
- 极简现代（Minimal Modern）
- 少即是多（无噪音）
- 高级阅读排版（Notion / Medium 风格）
- 卡片式信息组织（清晰分区 + 留白）

排版规范：
- 页面最大宽度 800px，居中
- 标题层级清晰，段落舒适
- 使用浅色背景、轻阴影、圆角卡片
- 不使用任何花哨颜色，强调色仅用于少量重点

------------------------------------------------------------
【输出目标】

生成的 HTML 应当可以直接在插件 iframe 中渲染，
呈现为一个高级、清爽、结构化的信息卡片页面，
让用户无需阅读全文即可快速理解核心内容。

------------------------------------------------------------
以下是网页文章内容：

\`\`\`
{{content}}
\`\`\`

请现在开始输出 HTML（你必须且只能输出 HTML 代码，不允许输出任何解释、描述、多余文字，用\`\`\`html...\`\`\`包裹输出的HTML代码）。`
  },
  {
    id: 'hierarchy',
    name: '🏗️ 层级结构',
    description: '按层级展示文章结构和要点',
    template: `你是一位专业的内容结构分析师。请将以下文章内容进行层级结构分析，输出一个清晰的层级结构可视化页面。

【输出要求】
1. 只输出 HTML 代码，用\`\`\`html...\`\`\`包裹
2. 使用缩进和视觉层级清晰展示文章结构
3. 每个层级用不同的样式区分
4. 保持极简现代风格

【设计规范】
- 最大宽度 800px，居中
- 使用左侧边线或缩进表示层级
- 浅色背景，柔和阴影
- 层级之间有清晰的视觉区分

以下是文章内容：

\`\`\`
{{content}}
\`\`\`

请输出 HTML：`
  },
  {
    id: 'comparison',
    name: '⚖️ 对比分析',
    description: '提取文章中的对比点，生成对比表格',
    template: `你是一位专业的内容分析师。请分析以下文章，提取其中的对比性内容，生成一个清晰的对比分析卡片。

【输出要求】
1. 只输出 HTML 代码，用\`\`\`html...\`\`\`包裹
2. 使用表格或左右对比布局
3. 突出对比的维度和差异点
4. 如果文章没有明显的对比内容，则提取正反两面观点

【设计规范】
- 最大宽度 900px，居中
- 对比双方使用不同的背景色区分
- 表格样式简洁现代
- 关键差异点高亮显示

以下是文章内容：

\`\`\`
{{content}}
\`\`\`

请输出 HTML：`
  },
  {
    id: 'action-items',
    name: '✅ 行动要点',
    description: '提取可执行的行动项和待办事项',
    template: `你是一位专业的行动规划师。请从以下文章中提取可执行的行动要点，生成一个清晰的行动清单卡片。

【输出要求】
1. 只输出 HTML 代码，用\`\`\`html...\`\`\`包裹
2. 提取具体可执行的行动项
3. 按优先级或时间顺序排列
4. 每个行动项包含简短描述

【设计规范】
- 最大宽度 700px，居中
- 使用复选框样式的列表
- 行动项之间有清晰分隔
- 优先级高的项目视觉突出

以下是文章内容：

\`\`\`
{{content}}
\`\`\`

请输出 HTML：`
  },
  {
    id: 'concept-card',
    name: '🎴 概念卡片',
    description: '生成固定尺寸的分享概念卡片',
    template: `你是一位专业的文章概念卡片设计师。

【核心尺寸要求】
- **固定尺寸**：1080px × 800px，任何内容都不得超出此边界
- **安全区域**：实际内容区域为1020px × 740px（四周预留30px边距）
- **溢出处理**：宁可减少内容，也不允许任何元素溢出边界

【输出要求】
1. 只输出 HTML 代码，用\`\`\`html...\`\`\`包裹
2. 必须设置 width: 1080px; height: 800px; overflow: hidden;
3. 内容必须精简，确保不溢出

【设计规范】
- 标题醒目，字号大
- 核心内容分区明确
- 底部可加来源信息
- 整体风格精致、适合分享

以下是文章内容：

\`\`\`
{{content}}
\`\`\`

请输出 HTML：`
  }
];

/**
 * 获取 Prompt 模板
 */
export function getPromptById(id) {
  return BUILTIN_PROMPTS.find(p => p.id === id);
}

/**
 * 填充 Prompt 模板变量
 */
export function fillPromptTemplate(template, variables) {
  let result = template;
  for (const [key, value] of Object.entries(variables)) {
    result = result.replace(new RegExp(`{{${key}}}`, 'g'), value);
  }
  return result;
}
